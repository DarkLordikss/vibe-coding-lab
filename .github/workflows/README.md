# CI/CD Pipeline Documentation

## Обзор

Этот CI/CD pipeline автоматизирует процесс сборки, тестирования и развертывания приложения системы управления больницей.

## Этапы Pipeline

### 1. Build (Сборка)
- Собирает Docker образ приложения
- Сохраняет образ как артефакт для последующих этапов

### 2. Unit Tests (Юнит-тесты)
- Запускает юнит-тесты локально с использованием Redis сервиса
- Проверяет корректность работы всех компонентов

### 3. Load Tests (Нагрузочное тестирование)
- Запускает Locust для нагрузочного тестирования
- Тестирует производительность приложения под нагрузкой
- Генерирует отчеты о производительности

### 4. Deploy (Мок-развертывание)
- Симулирует развертывание на VM через SSH
- Выводит в консоль все этапы деплоя (как в реальном деплое)
- Показывает процесс: бэкап, подготовка пакета, загрузка, настройка, проверка
- **Важно:** Это моковый деплой - реальные SSH команды не выполняются
- **Важно:** Деплой запускается только если все тесты прошли успешно

## Настройка Secrets

Для работы pipeline необходимо настроить следующие secrets в GitHub:

1. **SSH_PRIVATE_KEY** - Приватный SSH ключ для доступа к VM
2. **SSH_HOST** - IP адрес или доменное имя VM
3. **SSH_USER** - Имя пользователя для SSH подключения
4. **SSH_PORT** (опционально) - Порт SSH (по умолчанию 22)

### Как добавить secrets:

1. Перейдите в Settings → Secrets and variables → Actions
2. Нажмите "New repository secret"
3. Добавьте каждый secret с соответствующим именем

## Обработка ошибок

### При падении тестов:
- ❌ Pipeline завершается с ошибкой
- ❌ Деплой **не запускается** (блокируется через `needs`)
- ✅ Все артефакты сохраняются для анализа

### При ошибке деплоя:
- ⚠️ Pipeline завершается с ошибкой
- 📊 Результат отображается в summary

### При успешном деплое:
- ✅ Мок-деплой выполнен успешно
- 📝 Все этапы деплоя отображены в логах
- ✅ Pipeline завершен успешно

## Триггеры

Pipeline запускается автоматически при:
- Push в ветки `main`, `develop`, `master`
- Создании Pull Request в эти ветки
- Ручном запуске через `workflow_dispatch`

## Результаты

После выполнения pipeline:
- Артефакты тестов сохраняются на 1 день
- Отчеты нагрузочного тестирования доступны для скачивания
- Сводка выполнения отображается в Actions tab

## Локальный запуск

Для локального тестирования pipeline можно использовать [act](https://github.com/nektos/act):

```bash
# Установить act
curl https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash

# Запустить workflow
act push
```

## Troubleshooting

### Проблемы с SSH подключением
- Проверьте правильность SSH ключа
- Убедитесь, что ключ добавлен в authorized_keys на VM
- Проверьте настройки firewall

### Проблемы с Docker
- Убедитесь, что Docker установлен на VM
- Проверьте права доступа для Docker команд

### Проблемы с тестами
- Проверьте логи в Actions tab
- Убедитесь, что все зависимости установлены

